import numpy as np

flips = np.load("static/chainresults/flip_history.npy")


# filter later:
flips
from itertools import grouby
from itertools import groupby
flips = flips.tolist
flips = flips.tolist()
flips
flips = np.load("static/chainresults/flip_history.npy")
flips
flips = flips.tolist()
flips
from itertools import groupby
[len(list(group)) for key, group in groupby(flips)]
from collections import Counter
Counter(flips)
len(flips)
flips = np.load("static/chainresults/flip_history.npy")
len(flips)
flips = [tuple(x) for x in flips]
flips
Counter(flips)
flip_counts = Counter(flips)
flip_counts
typeof(flip_counts)
type(flip_counts)
flip_counts.items()
import readline
for i in range(readline.get_current_history_length()):
	print(readline.get_history_item(i+1)
)
for i in range(readline.get_current_history_length()):
	print(readline.get_history_item(i+1))
